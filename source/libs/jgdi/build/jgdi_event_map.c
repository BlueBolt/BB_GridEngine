/*___INFO__MARK_BEGIN__*/
/*************************************************************************
 *
 *  The Contents of this file are made available subject to the terms of
 *  the Sun Industry Standards Source License Version 1.2
 *
 *  Sun Microsystems Inc., March, 2001
 *
 *
 *  Sun Industry Standards Source License Version 1.2
 *  =================================================
 *  The contents of this file are subject to the Sun Industry Standards
 *  Source License Version 1.2 (the "License"); You may not use this file
 *  except in compliance with the License. You may obtain a copy of the
 *  License at http://gridengine.sunsource.net/Gridengine_SISSL_license.html
 *
 *  Software provided under this License is provided on an "AS IS" basis,
 *  WITHOUT WARRANTY OF ANY KIND, EITHER EXPRESSED OR IMPLIED, INCLUDING,
 *  WITHOUT LIMITATION, WARRANTIES THAT THE SOFTWARE IS FREE OF DEFECTS,
 *  MERCHANTABLE, FIT FOR A PARTICULAR PURPOSE, OR NON-INFRINGING.
 *  See the License for the specific provisions governing your rights and
 *  obligations concerning the Software.
 *
 *   The Initial Developer of the Original Code is: Sun Microsystems, Inc.
 *
 *   Copyright: 2001 by Sun Microsystems, Inc.
 *
 *   All Rights Reserved.
 *
 ************************************************************************/
/*___INFO__MARK_END__*/
/**
 *  Generated from c_event_map.jsp
 *  !!! DO NOT EDIT THIS FILE !!!
 */

#include <ctype.h>
#include <string.h>
#include <stdio.h>
#include <jni.h>
#include "jgdi.h"
#include "basis_types.h"
#include "cull.h"
#include "sgermon.h"
#include "sge_all_listsL.h"
#include "sge_answer.h"
#include "sge_event.h"
#include "jgdi_common.h"    
#include "jgdi_wrapper_event.h"


JNIEXPORT void JNICALL Java_com_sun_grid_jgdi_event_EventTypeMapping_nativeInit(JNIEnv *env, jclass clazz);

/*
 * Class:     com_sun_grid_jgdi_event_EventTypeMapping
 * Method:    nativeInit
 * Signature: ()V
 */
JNIEXPORT void JNICALL Java_com_sun_grid_jgdi_event_EventTypeMapping_nativeInit(JNIEnv *env, jclass clazz) {

    jgdi_result_t        ret = JGDI_SUCCESS;
    lList * alp = NULL;
    
    DENTER(BASIS_LAYER, "Java_com_sun_grid_jgdi_event_EventTypeMapping_nativeInit");


      ret = EventTypeMapping_static_registerEvent(env, "HostgroupAdd", sgeE_HGROUP_ADD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "HostgroupDel", sgeE_HGROUP_DEL, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "HostgroupMod", sgeE_HGROUP_MOD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "HostgroupList", sgeE_HGROUP_LIST, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "SubmitHostAdd", sgeE_SUBMITHOST_ADD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "SubmitHostDel", sgeE_SUBMITHOST_DEL, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "SubmitHostMod", sgeE_SUBMITHOST_MOD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "SubmitHostList", sgeE_SUBMITHOST_LIST, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ExecHostAdd", sgeE_EXECHOST_ADD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ExecHostDel", sgeE_EXECHOST_DEL, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ExecHostMod", sgeE_EXECHOST_MOD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ExecHostList", sgeE_EXECHOST_LIST, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ManagerAdd", sgeE_MANAGER_ADD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ManagerDel", sgeE_MANAGER_DEL, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ManagerMod", sgeE_MANAGER_MOD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ManagerList", sgeE_MANAGER_LIST, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "AdvanceReservationAdd", sgeE_AR_ADD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "AdvanceReservationDel", sgeE_AR_DEL, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "AdvanceReservationMod", sgeE_AR_MOD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "AdvanceReservationList", sgeE_AR_LIST, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "PETaskAdd", sgeE_PETASK_ADD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "PETaskDel", sgeE_PETASK_DEL, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "SchedConfMod", sgeE_SCHED_CONF, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ConfigurationAdd", sgeE_CONFIG_ADD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ConfigurationDel", sgeE_CONFIG_DEL, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ConfigurationMod", sgeE_CONFIG_MOD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ConfigurationList", sgeE_CONFIG_LIST, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ProjectAdd", sgeE_PROJECT_ADD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ProjectDel", sgeE_PROJECT_DEL, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ProjectMod", sgeE_PROJECT_MOD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ProjectList", sgeE_PROJECT_LIST, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ShareTreeMod", sgeE_NEW_SHARETREE, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "AdminHostAdd", sgeE_ADMINHOST_ADD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "AdminHostDel", sgeE_ADMINHOST_DEL, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "AdminHostMod", sgeE_ADMINHOST_MOD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "AdminHostList", sgeE_ADMINHOST_LIST, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "CheckpointAdd", sgeE_CKPT_ADD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "CheckpointDel", sgeE_CKPT_DEL, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "CheckpointMod", sgeE_CKPT_MOD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "CheckpointList", sgeE_CKPT_LIST, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ParallelEnvironmentAdd", sgeE_PE_ADD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ParallelEnvironmentDel", sgeE_PE_DEL, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ParallelEnvironmentMod", sgeE_PE_MOD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ParallelEnvironmentList", sgeE_PE_LIST, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "JobTaskAdd", sgeE_JATASK_ADD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "JobTaskDel", sgeE_JATASK_DEL, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "JobTaskMod", sgeE_JATASK_MOD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ComplexEntryAdd", sgeE_CENTRY_ADD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ComplexEntryDel", sgeE_CENTRY_DEL, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ComplexEntryMod", sgeE_CENTRY_MOD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ComplexEntryList", sgeE_CENTRY_LIST, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ClusterQueueAdd", sgeE_CQUEUE_ADD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ClusterQueueDel", sgeE_CQUEUE_DEL, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ClusterQueueMod", sgeE_CQUEUE_MOD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ClusterQueueList", sgeE_CQUEUE_LIST, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "JobSchedulingInfoAdd", sgeE_JOB_SCHEDD_INFO_ADD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "JobSchedulingInfoDel", sgeE_JOB_SCHEDD_INFO_DEL, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "JobSchedulingInfoMod", sgeE_JOB_SCHEDD_INFO_MOD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "JobSchedulingInfoList", sgeE_JOB_SCHEDD_INFO_LIST, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "UserSetAdd", sgeE_USERSET_ADD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "UserSetDel", sgeE_USERSET_DEL, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "UserSetMod", sgeE_USERSET_MOD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "UserSetList", sgeE_USERSET_LIST, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "CalendarAdd", sgeE_CALENDAR_ADD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "CalendarDel", sgeE_CALENDAR_DEL, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "CalendarMod", sgeE_CALENDAR_MOD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "CalendarList", sgeE_CALENDAR_LIST, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "OperatorAdd", sgeE_OPERATOR_ADD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "OperatorDel", sgeE_OPERATOR_DEL, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "OperatorMod", sgeE_OPERATOR_MOD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "OperatorList", sgeE_OPERATOR_LIST, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "JobAdd", sgeE_JOB_ADD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "JobDel", sgeE_JOB_DEL, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "JobMod", sgeE_JOB_MOD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "JobList", sgeE_JOB_LIST, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "QueueInstanceAdd", sgeE_QINSTANCE_ADD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "QueueInstanceDel", sgeE_QINSTANCE_DEL, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "QueueInstanceMod", sgeE_QINSTANCE_MOD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ResourceQuotaSetAdd", sgeE_RQS_ADD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ResourceQuotaSetDel", sgeE_RQS_DEL, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ResourceQuotaSetMod", sgeE_RQS_MOD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "ResourceQuotaSetList", sgeE_RQS_LIST, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "UserAdd", sgeE_USER_ADD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "UserDel", sgeE_USER_DEL, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "UserMod", sgeE_USER_MOD, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "UserList", sgeE_USER_LIST, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "QmasterGoesDown", sgeE_QMASTER_GOES_DOWN, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "SchedulerRun", sgeE_SCHEDDMONITOR, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "Shutdown", sgeE_SHUTDOWN, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "JobFinish", sgeE_JOB_FINISH, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "JobUsage", sgeE_JOB_USAGE, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "JobFinalUsage", sgeE_JOB_FINAL_USAGE, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "JobPriorityMod", sgeE_JOB_MOD_SCHED_PRIORITY, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "QueueInstanceSuspend", sgeE_QINSTANCE_SOS, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

      ret = EventTypeMapping_static_registerEvent(env, "QueueInstanceUnsuspend", sgeE_QINSTANCE_USOS, &alp);
      if(ret != JGDI_SUCCESS) {
          throw_error_from_answer_list(env, ret, alp);
          DRETURN_VOID;
      }

   DRETURN_VOID;
}
